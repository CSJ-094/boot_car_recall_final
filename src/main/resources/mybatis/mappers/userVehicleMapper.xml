<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.dao.UserVehicleDao">
    <insert id="save" parameterType="com.boot.dto.UserVehicleDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO USER_VEHICLE (USERNAME, MAKER, MODEL_NAME)
        VALUES (#{username}, #{maker}, #{modelName})
    </insert>

    <select id="findByUsername" parameterType="String" resultType="com.boot.dto.UserVehicleDto">
        SELECT ID, USERNAME, MAKER, MODEL_NAME
        FROM USER_VEHICLE
        WHERE USERNAME = #{username}
    </select>

    <select id="findAll" resultType="com.boot.dto.UserVehicleDto">
        SELECT ID, USERNAME, MAKER, MODEL_NAME
        FROM USER_VEHICLE
    </select>

    <delete id="delete" parameterType="long">
        DELETE FROM USER_VEHICLE WHERE ID = #{id}
    </delete>
</mapper>